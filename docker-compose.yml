services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gardenmap
      POSTGRES_USER: garden
      POSTGRES_PASSWORD: garden
    ports:
      - "5432:5432"
    volumes:
      - gardenmap_pgdata:/var/lib/postgresql/data

volumes:
  gardenmap_pgdata:
